# Python
__pycache__/
*.pyc
*.pyo

# Downloaded/generated model & inference caches (re-fetched on first run)
.tflite_cache/
.tfhub_cache/

# Local runtime data (per-machine, regenerated / grows over time)
eq_cache.db
preference_data.npz
ab_test_results.json

# Generated icon -- rebuild with generate_icon.py rather than tracking the
# binary; keeps diffs clean if the artwork changes.
SoundIntelligence.ico

# Fingerprint binary is platform-specific; keep out unless you want it vendored
# fpcalc.exe

# Editor / OS cruft
.vscode/
.idea/
Thumbs.db
.DS_Store

# Scratch / unrelated
Claude suggest/
